Ah, wait. If we abort `spawn_blocking`, tokio's documentation states that the thread is not cancelled, it just runs to completion detached.
Because the thread runs to completion detached, if it holds a `MutexGuard`, it will continue to hold the `MutexGuard` until it finishes.
To release the `MutexGuard` early, the query execution itself MUST be aware of cancellation.

We could add a parameter to `execute` to check a `Arc<AtomicBool>`, but that would require changing `execute`'s signature, which might affect `execute_query` in WASM or library usages. Wait, WASM could just pass a dummy flag. But maybe there's a better way?

If we change `execute(&mut self, query: &str)` to take no extra parameters, we can't easily cancel it. Wait, `execute` is `&mut self` on `Graph`. Wait, no, it's `&mut self`, but in `handle_query` we lock it: `let mut g = graph.lock().await;`

Wait, `Graph::execute` is defined as: `pub fn execute(&mut self, query_str: &str) -> Result<String, String>`

Wait! Does `execute` really take `&mut self`?
Yes, it does: `pub fn execute(&mut self, query_str: &str) -> Result<String, String>`.

Wait! The problem says: "When a user cancels a request (by closing the connection), stop the query it is running."
If we just pass a cancellation flag, how do we pass it?

Could we add `cancel_token: Option<Arc<AtomicBool>>` to `Graph::execute`?
Or we could add it to `Graph` struct? No, `Graph` is persisted to disk, so adding it there without `#[serde(skip)]` would be bad, but we could add `#[serde(skip)] pub cancel_flag: Option<Arc<AtomicBool>>` to `Graph`.
Then before executing, we set `g.cancel_flag = Some(flag.clone());`. Inside `execute_plan` or `execute` loops, we check `if let Some(flag) = &self.cancel_flag { if flag.load(Ordering::Relaxed) { return Err("Cancelled".to_string()) } }`. Or we can just break execution.

Wait! A much simpler way to pass context in Rust without changing signatures everywhere is to use a thread-local variable, but thread-locals don't cross async tasks well (though `execute` is synchronous).

Or, we can simply change `execute`'s signature, or add a method `execute_with_cancel(&mut self, query_str: &str, cancel_flag: Arc<AtomicBool>)`.
Then `execute` just calls `execute_with_cancel(query_str, Arc::new(AtomicBool::new(false)))`.
